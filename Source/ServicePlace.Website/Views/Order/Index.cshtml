@using ServicePlace.Website.Models.OrderViewModels
@model IEnumerable<PreviewViewModel>

@{
    Layout = "_Layout";
}

@foreach (var order in Model)
{
    <partial name="/Views/Partials/Order/Preview.cshtml" for="@order" />
}